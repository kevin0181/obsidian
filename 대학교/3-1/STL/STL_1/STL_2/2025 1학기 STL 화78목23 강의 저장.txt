2570========================================
저장 시간 : 03/11/25 15:44:59 Tuesday
========================================

//-------------------------------------------------
// 2025. STL 3.6                              1w 2d
//-------------------------------------------------
// 강의소개 : 한 학기 강의를 저장할 수 있는 save 기능
// 
// 과제 - save 기능을 save.cpp로 분리하라.
// 
// Release  x64
// 프로젝트 -> 속성 -> c++ 표준 최신으로 - std::c++latest
// c/c++ 에서 sdl검사 아니요로
//-------------------------------------------------
// 메모장
// C++ 사용하면서 사용하지 말아야 할 것들!
// 1. char*(문자열) -> string (으로 쓰셈ㅋ) -> string_view 같은거 쓸 줄 알아야함
// 2. T*  (문제가 너무 많아서 쓰지마라~) -> unique-ptr<T>, shared-ptr<T> 쓰셈
// 3. T[N] (정적 배열? 쓰지말라는듯?) -> array<T,N> 쓰셈
// 
//-------------------------------------------------
#include <iostream>
#include <filesystem>
#include <chrono>
#include <fstream>
#include <string>

#include "save.h"

void save(std::string_view);

int main()
{
    std::cout << "2025 STL 시작" << std::endl;

    save("main.cpp");
    return 0;
}2570============================================
저장 시간 : 03/11/25 15:46:11 Tuesday
============================================

//-------------------------------------------------
// 2025. STL 3.6                              1w 2d
//-------------------------------------------------
// 강의소개 : 한 학기 강의를 저장할 수 있는 save 기능
// 
// 과제 - save 기능을 save.cpp로 분리하라.
// 
// Release  x64
// 프로젝트 -> 속성 -> c++ 표준 최신으로 - std::c++latest
// c/c++ 에서 sdl검사 아니요로
//-------------------------------------------------
// 메모장
// C++ 사용하면서 사용하지 말아야 할 것들!
// 1. char*(문자열) -> string (으로 쓰셈ㅋ) -> string_view 같은거 쓸 줄 알아야함
// 2. T*  (문제가 너무 많아서 쓰지마라~) -> unique-ptr<T>, shared-ptr<T> 쓰셈
// 3. T[N] (정적 배열? 쓰지말라는듯?) -> array<T,N> 쓰셈
// 
//-------------------------------------------------
#include <iostream>
#include <filesystem>
#include <chrono>
#include <fstream>
#include <string>

#include "save.h"

void save(std::string_view);

int main()
{
    std::cout << "2025 STL 시작" << std::endl;

    save("main.cpp");
    return 0;
}2570========================================
저장 시간 : 03/11/25 15:48:31 Tuesday
========================================

//-------------------------------------------------
// 2025. STL 3.6                              1w 2d
//-------------------------------------------------
// 강의소개 : 한 학기 강의를 저장할 수 있는 save 기능
// 
// 과제 - save 기능을 save.cpp로 분리하라.
// 
// Release  x64
// 프로젝트 -> 속성 -> c++ 표준 최신으로 - std::c++latest
// c/c++ 에서 sdl검사 아니요로
//-------------------------------------------------
// 메모장
// C++ 사용하면서 사용하지 말아야 할 것들!
// 1. char*(문자열) -> string (으로 쓰셈ㅋ) -> string_view 같은거 쓸 줄 알아야함
// 2. T*  (문제가 너무 많아서 쓰지마라~) -> unique-ptr<T>, shared-ptr<T> 쓰셈
// 3. T[N] (정적 배열? 쓰지말라는듯?) -> array<T,N> 쓰셈
// 
//-------------------------------------------------
#include <iostream>
#include <filesystem>
#include <chrono>
#include <fstream>
#include <string>

#include "save.h"

int main()
{
    std::cout << "2025 STL 시작" << std::endl;

    save("main.cpp");
    return 0;
}2570========================================
저장 시간 : 03/11/25 15:59:36 Tuesday
========================================

//-------------------------------------------------
// 2025. STL 3.11                             1w 2d
//-------------------------------------------------
// 메모장
// C++ 사용하면서 사용하지 말아야 할 것들!
// 1. char*(문자열) -> string (으로 쓰셈ㅋ) -> string_view 같은거 쓸 줄 알아야함
// 2. T*  (문제가 너무 많아서 쓰지마라~) -> unique-ptr<T>, shared-ptr<T> 쓰셈
// 3. T[N] (정적 배열? 쓰지말라는듯?) -> array<T,N> 쓰셈
// 
//-------------------------------------------------
// [문제] main을 수정하면 안됨. 의도대로 실행되게 하자.
//
//

#include <iostream>

#include "save.h"

void change(int&, int&);

int main()
{
    
    int a{ 1 }, b{ 2 };
    change(a, b);
	std::cout << a << ", " << b << std::endl; // 의도 - 2, 1 출력되어야

    save("main.cpp");

    return 0;
}

void change(int& a, int& b) {
	int temp = a;
	a = b;
	b = temp;
}